# Screenshots

Referenced from the project [README.md](../README.md). **Excluded from the WordPress.org release ZIP** by `.github/workflows/release.yml`, so adding heavy images here does not bloat the plugin distribution.

Naming convention so the README can pick them up:

| File | What it shows |
|---|---|
| `01-records-list.jpg` | Records tab with live filters, integrity column and CSV export. |
| `02-settings.jpg` | Settings tab with editable texts, version control, retention, email options and uninstall control. |
| `03-order-metabox.jpg` | Order edit screen with the consent metabox (summary, integrity badge, certificate button). |
| `04-orders-column.jpg` | Consent column on the orders list. |
| `05-certificate.jpg` | Printable A4 certificate (the browser saves it as PDF). |
| `06-csv.jpg` | Exported CSV file with filtered records. |

Recommended size: 1600×1000 (or any 16:10 ratio) PNG, < 300 KB each.
